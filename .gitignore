# Legacy per-workout backup snapshots (no longer produced by the sync code).
# data/backup.json is the single canonical sync target and IS tracked.
backups/

# Sync server secrets/config
server/.env

# Tooling
node_modules/
.DS_Store
