{
  "name": "crimpy-sync-server",
  "version": "1.0.0",
  "description": "Raspberry Pi sync server for Crimpy: holds GitHub creds and 3-way-merges training data across devices.",
  "private": true,
  "main": "sync-server.js",
  "scripts": {
    "start": "node sync-server.js",
    "test": "node merge.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
